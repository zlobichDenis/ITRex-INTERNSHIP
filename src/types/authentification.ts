export type SignUpFormValues = {
  firstName: string;
  lastName: string;
  userName: string;
  password: string;
  passwordConfirm: string;
};

export type SignInFormValues = {
  userName: string;
  password: string;
};
